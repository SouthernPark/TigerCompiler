structure MakeGraph :
sig 
    (* val instrs2graph : Assem.instr list -> Flow.flowgraph * Flow.Graph.node list *)
end =
struct
    (* fun instrs2graph instrlist = 
    let
      bindings
    in
      body
    end *)

end