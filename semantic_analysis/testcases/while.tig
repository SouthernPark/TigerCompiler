let
  type rec = { val : int }
  type rec_arr = array of rec
  var  table := rec_arr[2] of nil
  var i:= 0
in
  while i<5 do 
    (table[i] := rec { val = 42 };i := i+1);
  table[0].val := 51
  /* table[1].val = 42. */
end